@model IEnumerable<crosam.Models.Location>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Ingresar Nueva Ubicación</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LocationName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LocationName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LocationID">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.LocationID">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.LocationID">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
