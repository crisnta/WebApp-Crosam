@model IEnumerable<crosam.Models.Cuelga>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CuelgaLenght)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CuelgaType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CuelgaLenght)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CuelgaType.CuelgaTypeId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CuelgaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CuelgaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CuelgaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
